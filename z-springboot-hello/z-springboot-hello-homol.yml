caller:
  destination:
    # z-springboot-hello from swagger.
    client0:
      resourceUri:
      clientId:
      clientSecret:
      scopes:
      registrationId:

## uri/enabled de gr-conf-swagger-aggregator
aggregator:
  eureka.enabled: true