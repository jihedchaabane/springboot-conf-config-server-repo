foo:
  msg: foo-local
  props:
    fooprop1: foo-local 1
    fooprop2: foo-local 2
    fooprop3: foo-local 3

## uri de gr-conf-swagger-aggregator
swagger:
  aggregator:
    cors:
      uri: http://localhost:8765

eureka:
  instance:
    metadata-map:
      # Indique que ce microservice doit être inclus dans le swagger de 'gr-conf-swagger-aggregator'' (désactiver le sinon sinon).
      swagger.aggregator.enabled: true